import type { Metadata } from "next"
import { MaincommercialTransaction } from "app/plus/commercialtransaction/components/MaincommercialTransaction"

/**
 * 特定商取引法に基づく表記のページ
 * @returns
 */
const CommercialTransactionPage = async () => {
  return <MaincommercialTransaction />
}

export const generateMetadata = async (): Promise<Metadata> => {
  return {
    robots: { index: false },
    title: { absolute: "特定商取引法に基づく表記" },
  }
}

export const revalidate = 240

export default CommercialTransactionPage
